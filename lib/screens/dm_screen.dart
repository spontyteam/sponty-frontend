import 'package:flutter/material.dart';

class DmScreen extends StatelessWidget {
  const DmScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('DM', style: TextStyle(fontSize: 24)));
  }
}
